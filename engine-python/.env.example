DEMO_MODE=1

# Optional: set DEMO_MODE=0 to use live data sources.
# Keep real secrets only in .env. Do not commit .env.
OPENAI_API_KEY=
APIFY_TOKEN=

# Optional model/actor overrides.
LLM_MODEL=gpt-4o-mini
EMBED_MODEL=text-embedding-3-small
APIFY_TIKTOK_ACTOR=clockworks/tiktok-scraper
APIFY_FB_ACTOR=apify/facebook-pages-scraper

